export interface IPrediction {
    predictionID: string;
    predictionStatus: string;
    prediction: string;
  }

export interface IPredictionResponse {
  id: string;
  status: string;
  prediction: string;
}